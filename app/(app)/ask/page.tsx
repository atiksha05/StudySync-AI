import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function AskPage() {
  return (
    <PlaceholderPage
      title="Ask AI"
      description="Chat with Gemini across your entire knowledge workspace. Open any workspace and use the Ask AI tab for contextual answers."
    />
  );
}
